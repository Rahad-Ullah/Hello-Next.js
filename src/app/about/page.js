import styles from "./About.module.css";

const AboutPage = () => {
  return (
    <div>
      <h1 className={`${styles.heading} text-3xl text-center font-bold`}>
        About page
      </h1>
    </div>
  );
};

export default AboutPage;
